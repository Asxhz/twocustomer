# Runtime deps for the Vercel (serverless) agent deploy.
# playwright is intentionally omitted — Browserbase scraping degrades
# gracefully (lazy import) and the monitor cadence runs on a long-lived host.
fastapi>=0.115
anthropic>=0.40
httpx>=0.27
pydantic>=2.9
pydantic-settings>=2.5
python-dotenv>=1.0
sse-starlette>=2.1
apscheduler>=3.10
python-multipart>=0.0.9
sentry-sdk[fastapi]>=2.0
pynacl>=1.5
defusedxml>=0.7.1
