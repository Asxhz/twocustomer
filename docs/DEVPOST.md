# TwoCustomer

A forward-deployed AI teammate for consumer brands: it monitors every signal
surface, interviews customers (chat / voice / video), and fixes broken sites in a
safe sandbox (diff + PR + live preview).

See `README.md` for the overview, `docs/ARCHITECTURE.md` for the system design,
and `docs/DEMO.md` for the demo path.
